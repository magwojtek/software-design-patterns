export enum PatternType {
    // creational
    Builder = 'builder',
    Factory = 'factory',
    Prototype = 'prototype',
    Singleton = 'singleton',
    // structural

    // behavioral
    Observer = 'observer',
}
