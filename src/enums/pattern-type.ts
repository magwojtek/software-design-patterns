export enum PatternType {
    // creational
    Builder = 'builder',
    Factory = 'factory',
    Prototype = 'prototype',
    Singleton = 'singleton',

    // structural
    Adapter = 'adapter',
    Bridge = 'bridge',
    Composite = 'composite',
    Decorator = 'decorator',
    Facade = 'facade',
    Flyweight = 'flyweight',
    Proxy = 'proxy',

    // behavioral
    Observer = 'observer',
    Strategy = 'strategy',
    Iterator = 'iterator',
    Command = 'command',
}
